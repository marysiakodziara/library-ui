export enum ROUTES {
    HOME = "/",
    BOOK = "/book/:bookId",
    CHECKOUT = "/checkout",
    ACCOUNT = "/account",
    SEARCH = "/search/:phrase/:page",
    CATEGORIES = "/:categories/:page",
    BOOK_CLUB = "/book-club",
    ADMIN = "/admin",
}